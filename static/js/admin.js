async function adminApp(){const root=$('#admin-app');root.innerHTML=`<div class="page-heading"><div><p class="eyebrow">ADMINISTRATION</p><h1>System controls</h1><p class="muted">Manage users, employees, categories, and policies.</p></div></div><div class="grid"><section class="panel"><div class="panel-head"><h2>Users</h2></div><div class="table-wrap"><table><thead><tr><th>ID</th><th>Username</th><th>Email</th><th>Role</th><th>Status</th></tr></thead><tbody id="users"></tbody></table></div></section><section class="panel"><div class="panel-head"><h2>Employees</h2></div><div class="list" id="employees"></div></section></div><div class="grid"><section class="panel"><h2>Expense categories</h2><div id="categories" class="list"></div></section><section class="panel"><h2>Active policies</h2><div id="policies" class="list"></div></section></div>`;try{const [users,employees,categories,policies]=await Promise.all([apiFetch('/users'),apiFetch('/employees'),apiFetch('/categories'),apiFetch('/policies')]);$('#users').innerHTML=users.map(x=>`<tr><td>${x.id}</td><td>${esc(x.username)}</td><td>${esc(x.email)}</td><td>${esc(x.role)}</td><td>${x.is_active?'Active':'Inactive'}</td></tr>`).join('');$('#employees').innerHTML=employees.map(x=>`<div class="row"><div class="row-content"><strong>${esc(x.first_name+' '+x.last_name)}</strong><small>${esc(x.department)} / ${esc(x.designation)}</small></div></div>`).join('');$('#categories').innerHTML=categories.map(x=>`<div class="row"><strong>${esc(x.name)}</strong><span class="muted">${esc(x.description||'')}</span></div>`).join('');$('#policies').innerHTML=policies.map(x=>`<div class="row"><strong>Category ${x.category_id}</strong><span class="muted">Max ${money(x.max_amount)}</span></div>`).join('')}catch(ex){toast(ex.message)}}if(['ADMIN','SYSTEM_ADMIN'].includes(document.body.dataset.role))adminApp();
